# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DemoApplication' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

pod 'CWRateKit'

  # Pods for DemoApplication
     pod 'Cosmos'
     pod  'TinyConstraints'
  target 'DemoApplicationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DemoApplicationUITests' do
    # Pods for testing
  end

end
